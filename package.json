{
  "name": "team_sync",
  "version": "1.0.0",
  "description": "Team formation application",
  "scripts": {
    "build": "npm install -g @vue/cli && cd frontend/team_sync_front && npm install --legacy-peer-deps && npm run build && cd ../.. && mkdir -p static && cp -r frontend/team_sync_front/dist/* static/",
    "start": "python backend/app.py"
  },
  "engines": {
    "node": "18.x",
    "npm": "9.x"
  }
} 