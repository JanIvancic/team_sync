{
  "name": "team-sync",
  "version": "1.0.0",
  "description": "Team formation application",
  "engines": {
    "node": "16.x",
    "npm": "6.14.18"
  },
  "scripts": {
    "start": "gunicorn backend.app:app",
    "heroku-postbuild": "cd frontend/team_sync_front && npm install --production=false && npm run build && cd ../.. && mkdir -p static && cp -r frontend/team_sync_front/dist/* static/"
  }
} 