{
  "name": "team-sync",
  "version": "1.0.0",
  "description": "Team formation application",
  "main": "index.js",
  "scripts": {
    "heroku-prebuild": "cd frontend/team_sync_front && npm install",
    "heroku-postbuild": "bash build.sh",
    "start": "gunicorn app:app"
  },
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.0.0"
  },
  "dependencies": {
    "gunicorn": "^20.1.0"
  }
} 