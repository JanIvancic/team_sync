{
  "name": "team-sync",
  "version": "1.0.0",
  "description": "Team formation application",
  "main": "index.js",
  "scripts": {
    "heroku-prebuild": "cd frontend/team_sync_front && npm install && npm install @vue/cli-service",
    "heroku-postbuild": "cd frontend/team_sync_front && npm run build && cd ../.. && mkdir -p static && cp -r frontend/team_sync_front/dist/* static/",
    "start": "gunicorn app:app"
  },
  "engines": {
    "node": "14.21.3",
    "npm": "6.14.18"
  }
} 